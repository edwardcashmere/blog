---
- hosts: localhost
  become: true
  remote_user: root

  tasks:
    - name: Generate Secret
      ansible.builtin.shell: mix phx.gen.secret
      args:
        chdir: /Users/dev/personal/blog/omondo_blog
      register: secret_key

    - name: Second Task - Print the full output
      ansible.builtin.debug:
        var: secret_key








    
