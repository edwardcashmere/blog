---
- hosts: localhost
  become: true
  remote_user: edwin_ansible

  pre_tasks: 
    - name: update repositories
      apt:
        update_cache: yes
      changed_when: False

  tasks:
    - name: create project directory
      command: 
        cmd: mkdir -p /var/www/blog


    
